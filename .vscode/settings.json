{
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": "./src/.*",
                "isAsync": true,
                "cmd": "curl localhost:8000" // "npm run run ${file}"
            }
        ]
    }
}